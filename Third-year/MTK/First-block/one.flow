import runtime;
import ds/array;
import string;

main() {
    int_arr : [int] = [1, 2, 3];
    str_arr : [string] = map(int_arr, \i -> i2s(i));
    str : string = strGlue(str_arr, ",");
    println(str);
}

